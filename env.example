# Monarc Docker Environment Variables
# Copy this file to .env and set your passwords
# WARNING: Do not commit .env file to version control

# OPTIONAL: Database passwords (defaults will be used if not set)
DB_PASSWORD=your_secure_password_here
DB_ROOT_PASSWORD=your_secure_root_password_here

# Database configuration (optional, defaults shown)
DB_USER=monarc
DB_CLI_NAME=monarc_cli
DB_COMMON_NAME=monarc_common

# Application environment (optional)
APPLICATION_ENV=production

# Advanced settings (usually not needed)
# PATH_TO_MONARC=/var/lib/monarc/fo
# DB_HOST=db
# DB_PORT=3306

# Security recommendations:
# 1. Use strong, unique passwords
# 2. Change default passwords in production
# 3. Consider using Docker secrets in production environments
# 4. Regularly update passwords according to your security policy

# Docker Compose will automatically use these variables
# If .env is not created, default passwords will be used:
# DB_PASSWORD=monarc_password, DB_ROOT_PASSWORD=root_password
# To start: docker-compose up -d
